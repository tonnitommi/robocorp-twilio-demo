tasks:
  Produce:
    robotTaskName: Produce items
  Consume:
    robotTaskName: Consume items
  ReadNotifyLog:
    robotTaskName: Appointment remind

artifactsDir: output
condaConfigFile: conda.yaml
ignoreFiles: [.gitignore]

PATH: [.]
PYTHONPATH: [.]
